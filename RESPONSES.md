Codex did a good job fetching the maximum amount of data with minimum calls from figma MCP. The MCP output was very helpful for exact spacing, colors, typography, and layout dimensions.

The main friction was with the limited MCP calls available per account. Because of that, I had to plan each MCP call carefully and spend extra Codex time working from saved design details instead of repeatedly querying Figma

Next time, I would capture the key MCP outputs earlier, verify the rendered UI against the design and keep a tighter checklist for design tokens.